# 저장소 지침

## 환경 정보
- 운영체제: Windows
- shell: PowerShell 5.1 (Desktop)
- 프로젝트 루트: `D:\work\testCommand001`
- 주요 런타임: `python` `C:\Users\kb102\AppData\Local\Microsoft\WindowsApps\python.exe`, `node` `C:\Program Files\nodejs\node.exe`, `npm` `C:\Program Files\nodejs\npm.ps1`, `git` `UNKNOWN`

## 작업 원칙
- 작업 범위는 현재 프로젝트 루트와 그 하위 파일로만 제한한다.
- 프로젝트 내부 파일을 직접 읽고 판단하며, 추정으로 구현하지 않는다.
- 요구사항이 모호하면 짧게 확인하고, 확인이 어렵다면 안전한 최소 범위로 작업한다.
- 필요한 경우 먼저 짧은 계획을 세운 뒤 작업한다.
- 코드 수정 후에는 관련 테스트 또는 최소 검증을 수행한다.
- 테스트를 실행할 수 없으면 그 이유를 명확히 기록한다.
- 변경 내용과 검증 결과는 마지막에 짧게 요약한다.
- `.env`, secret, credential, API key, 인증 파일은 출력하거나 수정하지 않는다.
- 프로젝트 스코프 밖 파일은 읽거나 수정하지 않는다.
- 삭제, 강제 덮어쓰기, 원격 push 같은 파괴적 작업은 수행하지 않는다.
- 사용자가 제공한 변수명, 함수명, 파일명은 임의로 바꾸지 않는다.
- 불필요한 대규모 리팩토링은 하지 않는다.
- 요청 범위 밖의 기능은 임의로 추가하지 않는다.

## 보고 방식
- 이후 Codex의 설명, 계획, 보고는 기본적으로 한국어로 작성한다.
- 최종 응답에는 생성하거나 수정한 파일 목록만 간단히 보고한다.
